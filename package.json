{
  "name": "moving-image",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "npx electron .",
    "package-win": "electron-packager . electron-app --platform=win32 --arch=x64 --out=dist --electron-version=31.6.0 --icon=favicon.ico --overwrite"
  },
  "devDependencies": {
    "electron": "^31.6.0",
    "electron-packager": "^17.1.2"
  }
}
